//this exports a component with a loading indicator, which is rendered in place of the page when the data is being fetched. Results in faster load time,  making it easy to handle the error state

export default function Loading() {
  return <p>Loading...</p>;
}
